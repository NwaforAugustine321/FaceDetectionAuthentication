{
  "name": "facedetectionauthetication",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:build": "tsc",
    "dev:watch": "NODE_ENV=development nodemon -e ts -w . -w ./build build/index.js",
    "serve": "npm run dev:build && npm run dev:watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "nodemon": "^2.0.20"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13"
  }
}
